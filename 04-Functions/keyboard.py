###
# Functions to read any data type from the keyboard
#
def input_string(message):
    x = str(input(message))
    return x

def input_integer(message):
    x = int(input(message))
    return x

def input_real(message):
    x = input_real(message)
    return x

def input_boolean(message):
    x = bool(input(message))
    return x
